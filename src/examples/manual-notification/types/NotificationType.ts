/**
 * Enum defining all supported notification types
 * Same as the automated example, but without library benefits
 */
export enum NotificationType {
  EMAIL = 'email',
  SMS = 'sms', 
  PUSH = 'push',
  SLACK = 'slack',
  WEBHOOK = 'webhook'
} 